name: github-action-practice

on:
  pull_request:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo 'Hello, Github Actions'
        
      - name: Show Date
        run: date
