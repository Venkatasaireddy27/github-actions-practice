name: github-action-practice

on:
  push:
    branches: [main]

jobs:
  hello:
  runs-on: ubuntu-latest
  steps:
    - name: Say Hello
      run: echo 'Hello, Github Actions'
      
    - name: Show Date
      run: date
